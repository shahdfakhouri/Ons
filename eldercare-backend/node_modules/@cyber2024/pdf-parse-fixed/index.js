const Fs = require('fs');
const Pdf = require('./lib/pdf-parse-fixed.js');

module.exports = Pdf;

// let isDebugMode = process.env.DEBUG_PDF_PARSE == 'true'; 


//for testing purpose
// if (isDebugMode) {  
//     console.log({isDebugMode})

//     let PDF_FILE = './test/data/05-versions-space.pdf';
//     let dataBuffer = Fs.readFileSync(PDF_FILE);
//     Pdf(dataBuffer).then(function(data) {
//         Fs.writeFileSync(`${PDF_FILE}.txt`, data.text, {
//             encoding: 'utf8',
//             flag: 'w'
//         });
//         debugger;
//     }).catch(function(err) {
//         debugger;
//     });

// }
